SETUP:

Canvas drawing requires files to be hosted on a server. Assuming Python is installed, simply:

1. cd /directory_with_convnet_files/
2. python -m SimpleHTTPServer
3. navigate to localhost:8000/image_regression.html